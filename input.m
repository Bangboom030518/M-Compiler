import module;

function money() {
    callback(|| callback:{
        return@callback "hello!";
        result.unwrap_or(return None)
        result.or_break();
        (return@callback? result).sus;
    })
}